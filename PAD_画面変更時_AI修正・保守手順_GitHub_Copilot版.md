# Power Automate Desktop（PAD）画面変更時のAI修正・保守手順
## GitHub Copilot活用版（Gitは任意）

## 1. 目的

Webシステムや業務アプリの画面構造が変更され、Power Automate for desktop（PAD）のフローでエラーが発生した場合に、変更前後の画面と現在のPADフローをGitHub Copilotに渡し、以下をAIに分析・提案してもらう。

- 画面の変更点
- 影響を受けるPADアクション
- エラー原因
- UI要素・セレクターの修正案
- PAD上での具体的な修正方法
- 修正後のテスト項目
- 手順書・テスト証跡・変更履歴の更新案

実際のPAD修正と最終確認は人が行い、AIによる自動変更を前提としない。
また、Gitの利用は必須とせず、まずはローカルフォルダー＋GitHub Copilotだけでも運用できる構成とする。

---

## 2. 基本方針

### 最低限の入力

AIへ渡す情報は、原則として次の4点とする。

1. 変更前の画面スクリーンショット
2. 変更後の画面スクリーンショット
3. 現在のPADフロー
4. PADで発生したエラー内容

必要に応じて、対象UI要素やセレクター情報も追加する。

### 安全性

- AIの提案をそのまま本番フローへ反映しない
- AIに不明な情報を推測させない
- PADの修正は担当者が確認して実施する
- 修正後は必ずテストする
- パスワード、アクセストークン、秘密鍵などの機密情報をAIへ渡さない
- 業務データを含むスクリーンショットは、社内ルールに従ってマスキングする

---

# 3. 標準手順

## STEP 1　画面変更とPADエラーを確認

Webシステム等の画面変更によってPADがエラーになったことを確認する。

### 確認するもの

- 変更前の画面
- 変更後の画面
- PADのエラーメッセージ
- エラーが発生したアクション

### 代表的なエラー例

- UI要素が見つからない
- ボタンがクリックできない
- 入力欄が見つからない
- Webページの読み込み待ちでタイムアウト
- セレクターが一致しない

---

## STEP 2　AIへ渡す情報を準備

以下を用意する。

```text
① 変更前スクリーンショット
② 変更後スクリーンショット
③ 現在のPADフロー
④ エラー内容
```

### PADフローの取得

PADのフローデザイナーから対象のサブフローのアクションをコピーし、テキストとしてAIへ渡す。

可能であれば、UI要素・セレクター情報も含める。

---

## STEP 3　GitHubの利用は任意

### A. 最初はGitを使わない場合

ローカルフォルダーに以下を保存する。

```text
PAD-保守/
├─ flows/
│  └─ 対象フロー.txt
├─ screenshots/
│  ├─ before.png
│  └─ after.png
├─ procedures/
│  └─ 操作手順.md
├─ tests/
│  └─ テスト結果.md
└─ changes/
   └─ 変更履歴.md
```

この状態でも、VS CodeでGitHub Copilotを使ってファイルを読み込み、分析・更新できる。

### B. Gitを利用する場合

上記フォルダーをGitリポジトリとして管理する。

```text
PAD-保守/
        ↓
GitHub Repository
        ↓
履歴・差分・変更前後を管理
```

Gitを使う場合は、修正前の状態を残してからCopilotに変更させると、変更内容を比較しやすい。

**Gitは「必須」ではなく、「履歴・差分を残したい場合の追加機能」として扱う。**

---

# 4. STEP 4　GitHub Copilotへ修正を依頼

VS Code等で対象フォルダーを開き、GitHub Copilotへ以下のように依頼する。

## 基本プロンプト

```text
以下の情報をもとに、Power Automate for desktop（PAD）の
画面変更によるエラー原因と修正方法を分析してください。

【入力情報】
1. 変更前の画面
2. 変更後の画面
3. 現在のPADフロー
4. エラー内容

【依頼内容】
以下を順番に整理してください。

1. 画面の変更点
2. 影響を受ける可能性が高いPADアクション
3. エラー原因
4. UI要素・セレクターの問題点
5. PADでの具体的な修正方法
6. 修正後のテスト項目
7. 手順書・テスト証跡・変更履歴で更新すべき内容

不明な点は推測せず、「確認が必要」としてください。
実際のPADフローを直接変更するのではなく、まず修正案を提示してください。
既存の手順書や証跡は、今回の変更に必要な部分だけ更新してください。
```

---

# 5. STEP 5　Copilotの分析結果を確認

Copilotには、少なくとも以下を回答させる。

### ① 変更点

例：

```text
「検索」ボタンが「検索する」に変更された。
```

### ② 影響アクション

例：

```text
アクション4
「Webページ上のボタンをクリックする」
```

### ③ 原因

例：

```text
ボタンのテキストを使用したセレクターが
変更後の画面と一致しなくなった可能性が高い。
```

### ④ 修正方法

例：

```text
対象UI要素を再取得する。
必要に応じてRepair selectorを使用する。
安定した属性（id、aria-label等）が利用できる場合は
それを優先する。
```

### ⑤ テスト

例：

```text
- セレクターのテスト
- フロー全体の実行
- 処理結果の確認
```

---

# 6. STEP 6　AIが作成した手順書・証跡の変更案を確認

Copilotに、分析結果をもとに以下の更新案を作成させる。

### 更新対象

- 操作手順書
- テスト項目・テスト結果
- 変更履歴
- 必要に応じてPADフローの説明資料

### 基本方針

既存の文書を大きく作り変えず、

> 「今回変更された箇所だけ更新する」

ことを基本とする。

---

# 7. STEP 7　変更内容を人が確認

AIが作成した変更内容を確認する。

特に以下を確認する。

- PADの実際の動作と矛盾していないか
- AIが存在しないUI要素を想定していないか
- セレクターの修正内容が妥当か
- 手順書の変更が必要以上に大きくなっていないか
- テスト項目に漏れがないか
- 機密情報が文書に含まれていないか

Gitを使用する場合は、変更前後の差分（diff）を確認する。

GitHubのPull Requestでは変更ファイルと差分を確認できるため、AIが作成した変更を人がレビューしてから反映する運用が可能。

---

# 8. STEP 8　PADを実際に修正

Copilotの修正案を参考に、PAD上で担当者が修正する。

### 優先順位

1. UI要素を再取得
2. Repair selectorを試す
3. セレクターを確認・修正
4. 必要に応じて待機処理を追加
5. 最後に座標クリック等を検討

座標クリックへの安易な置き換えは避ける。

---

# 9. STEP 9　テスト

修正後、以下を確認する。

```text
□ 対象UI要素が取得できる
□ セレクターのテストが成功する
□ 対象アクションが正常に動作する
□ フロー全体が正常終了する
□ 処理結果が正しい
□ エラーが再発しない
```

可能であれば、テスト実行後の画面をスクリーンショットとして保存する。

---

# 10. STEP 10　手順書・証跡をAIで更新

テスト結果をCopilotへ渡し、文書を更新する。

### 依頼例

```text
今回のPAD修正とテスト結果をもとに、
以下の文書を必要最小限だけ更新してください。

- 操作手順書
- テスト項目・テスト結果
- 変更履歴

既存の内容はできるだけ維持してください。
今回変更された箇所だけ更新してください。
実際に確認できていない内容は追加しないでください。
```

AIが更新した内容を人が確認する。

---

# 11. Gitを使う場合の管理方法

Gitを使う場合は、以下程度のシンプルな運用でよい。

```text
修正前
  ↓
AIによる分析・文書更新
  ↓
差分確認
  ↓
PAD修正
  ↓
テスト
  ↓
テスト結果・証跡追加
  ↓
コミット
```

より厳格に管理する場合は、

```text
main
  │
  └─ 修正ブランチ
       ↓
   Copilotで更新
       ↓
   Pull Request
       ↓
   人が差分確認
       ↓
   PADテスト
       ↓
   マージ
```

とする。

GitHubのPull Requestでは、変更前後の差分を確認してからレビュー・マージできる。Copilotが作成したPull Requestについても、人による確認を行うことが推奨されている。

---

# 12. 推奨する最終運用

最初から複雑なGit運用を導入する必要はない。

## 最小構成

```text
PAD
 ↓
変更前/変更後スクショ
＋
PADフロー
＋
エラー
 ↓
GitHub Copilot
 ↓
修正案
＋
手順書更新案
＋
テスト項目
＋
変更履歴
 ↓
人が確認
 ↓
PAD修正
 ↓
テスト
 ↓
証跡更新
```

## Gitを利用できる場合

上記に、

```text
Git
 ↓
修正前の状態を保存
 ↓
Copilotによる変更
 ↓
差分確認
 ↓
テスト
 ↓
コミット／Pull Request
```

を追加する。

**Gitを使える環境では利用を推奨するが、Gitなしでも同じ保守手順を実施できるように設計する。**

---

# 13. 管理する成果物

最終的には、以下の4種類を最低限管理する。

| 成果物 | 内容 | AI更新 |
|---|---|---|
| PADフロー情報 | 現在のアクション・UI要素・セレクター | △ |
| 操作手順書 | PADの操作・注意点 | ○ |
| テスト証跡 | テスト項目・結果・スクショ | ○ |
| 変更履歴 | 変更理由・変更内容・影響範囲 | ○ |

PAD本体の修正は担当者が実施し、AIは主に「分析・修正案・文書更新」を担当する。

---

# 14. この運用の考え方

この仕組みの目的は、AIにPADを完全自動修正させることではない。

> **画面変更が起きたときに、担当者が「何が壊れたのか」「どこを直せばよいのか」「何をテストすればよいのか」をAIに整理してもらい、PADの修正と最終判断は人が行う。**

さらに、手順書・テスト証跡・変更履歴もAIに更新させることで、保守担当者の作業を減らす。

---

## 15. 目指す運用イメージ

```text
【画面変更】
      ↓
【PADエラー】
      ↓
【4点セットを準備】
  ・変更前画面
  ・変更後画面
  ・PADフロー
  ・エラー
      ↓
【GitHub Copilot】
      ↓
  変更点分析
  エラー原因
  PAD修正案
  テスト項目
  手順書更新案
  証跡更新案
      ↓
【人が確認】
      ↓
【PAD修正】
      ↓
【テスト】
      ↓
【AIで文書・証跡更新】
      ↓
【人が最終確認】
      ↓
【完了】

※ Gitが利用可能な場合は、
   修正前後の差分・履歴管理に利用する。
```

---

## 16. 重要な注意事項

- AIの回答は必ず担当者が確認する。
- AIが推測した内容を事実として手順書に記載しない。
- PADの本番フローを直接AIに変更させない。
- パスワード、秘密鍵、アクセストークン等をAIへ入力しない。
- スクリーンショットに機密情報・個人情報が含まれる場合はマスキングする。
- テストせずに修正完了としない。
- Gitを使う場合も、コミット・マージ前に変更差分を確認する。
- GitHub Copilotの提案だけで安全性を判断しない。

---

## 参考

Microsoft / GitHubの公式ドキュメントでは、Copilotによる変更について人がレビューすること、Pull Requestの差分を確認すること、VS Code上の未コミット変更をCopilotでレビューすることなどが案内されている。

- GitHub Copilotからの出力を確認する
- GitHub Copilotによるコードレビュー
- Pull Requestの変更差分レビュー
- VS CodeでのCopilot利用

本手順は、これらの仕組みをPADの保守・手順書更新・テスト証跡更新に応用した運用案である。
