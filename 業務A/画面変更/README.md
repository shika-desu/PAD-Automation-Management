# 画面変更資料

画面変更が発生したとき、変更前後の画面をこのフォルダに保存する。

- `before.png`: 画面変更前のスクリーンショット
- `after.png`: 画面変更後のスクリーンショット
- ファイル名は固定する。
- 必要に応じて日付別のサブフォルダを作ってよい。ただし、最初から複雑なフォルダ構成を必須にはしない。
- パスワード、トークン、顧客情報などの機密情報が含まれる場合は、保存前にマスキングする。
- 画像だけで判断できない場合は、エラー内容や確認日時などを別途記録する。
